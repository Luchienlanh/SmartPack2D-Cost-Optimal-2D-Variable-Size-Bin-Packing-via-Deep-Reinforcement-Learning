The instances are for the Variable Size bin packing problem.

Each instance has in the first line:

Number of pieces Number of bins 1

Dim_x Dim_y Dim_z Cost //of each bin  (if it is a 2D instance only dim_x and dim_y)

....

Number_piece Dim_x Dim_y Dim_z //of each piece